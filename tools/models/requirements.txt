onnx>=1.16
onnxsim>=0.4
numpy>=1.26
opencv-python>=4.9
tf2onnx>=1.16
tensorflow-cpu>=2.16,<2.20
paddle2onnx<2
paddlepaddle>=3.0
obsws-python>=1.7
# The DirectML wheel exists only on Windows; every other platform uses the CPU
# build. Both provide the same onnxruntime module that the conversion scripts use
# to verify a converted graph actually runs.
onnxruntime-directml>=1.20; sys_platform == "win32"
onnxruntime>=1.20; sys_platform != "win32"
